function [] = player()
%call function, display it
%call function 

%all_inputs = 
while true
    word = input('Type word Here:    ', "s");
    %function that checks whether word or not
    word_checking(word);
    count_points(word);
    

    %array for points
end
  
%initilzie empty list/array and populate with player entries
%while loop while true (for player to 
%exits program when timer is out

