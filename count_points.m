function [points] = count_points(word);

for iter = 1:length(word);
end
    

points = 0 + length(word);

for iter = 1:length(word)
    %points = regexpi(word, '[a-zA-Z]'))
    points = points + 1;
end

